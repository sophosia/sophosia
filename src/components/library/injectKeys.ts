/**
 * These are provided by ProjectBrowser.vue and injected into its child components
 */
export const KEY_metaDialog = Symbol(); // update project
export const KEY_deleteDialog = Symbol(); // delete project
